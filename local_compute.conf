[[local|localrc]]

# ==========================================
# OPENSTACK DEPLOYMENT CREDENTIALS
# ==========================================
# MUST MATCH THE CONTROL NODE CREDENTIALS
ADMIN_PASSWORD=your_secure_password
DATABASE_PASSWORD=$ADMIN_PASSWORD
RABBIT_PASSWORD=$ADMIN_PASSWORD
SERVICE_PASSWORD=$ADMIN_PASSWORD
SERVICE_TOKEN=$ADMIN_PASSWORD

# ==========================================
# NETWORK AND MULTI-NODE SETTINGS
# ==========================================
# Replace with the actual Tailscale IP of this Compute Node (Ubuntu 26), e.g., 100.99.10.30
HOST_IP=100.99.10.30

# Replace with the actual Tailscale IP of the Control Node (Mini PC), e.g., 100.99.10.20
SERVICE_HOST=100.99.10.20

# Enable Multi-Host deployment
MULTI_HOST=1

# ==========================================
# SERVICES ON COMPUTE NODE
# ==========================================
# Enable only the hypervisor (n-cpu) and the Neutron networking L2 agent (q-agt)
ENABLED_SERVICES=n-cpu,n-api-meta,placement-client,neutron,q-agt

# ==========================================
# CONNECTIONS TO THE CONTROL NODE
# ==========================================
# Tell this compute node where to find database, message queue, and API services
MYSQL_HOST=$SERVICE_HOST
RABBIT_HOST=$SERVICE_HOST
GLANCE_HOSTPORT=$SERVICE_HOST:9292
KEYSTONE_AUTH_HOST=$SERVICE_HOST
KEYSTONE_SERVICE_HOST=$SERVICE_HOST

# ==========================================
# NETWORK CUSTOMIZATIONS
# ==========================================
# Use Open vSwitch (OVS) for network tunneling.
# Must match the Control Node settings
Q_AGENT=openvswitch
Q_TUNNEL_TYPES=vxlan

# Tailscale MTU adjustment (Tailscale standard MTU is 1280, VXLAN overlay needs -50 bytes)
global_physnet_mtu=1200

# ==========================================
# LOGGING CONFIGURATION
# ==========================================
LOGFILE=/opt/stack/logs/stack.sh.log
LOG_COLOR=False
LOGDIR=/opt/stack/logs
